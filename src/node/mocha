<%>src/assets/fileheader</%>

###
# mocha
###

ifndef MOCHA_REPORTER
	MOCHA_REPORTER := spec
endif

ifndef MOCHA_TIMEOUT
	MOCHA_TIMEOUT := 2000
endif

ifndef MOCHA_SLOW
	MOCHA_SLOW := 75
endif

ifndef MOCHA_HELP
	MOCHA_HELP := "  mocha                         Test with mocha."
endif

# Targets

mocha:
	@node node_modules/.bin/mocha --timeout $(MOCHA_TIMEOUT) --slow $(MOCHA_SLOW) --reporter $(MOCHA_REPORTER)
	$(PRINT) "Mocha Test Finished!"

mocha-help:
	@echo ""
	@echo "<%>src/assets/help-headline</%>"
	@echo ""
	@echo "Usage:"
	@echo ""
	@echo "  mocha                       Run the tests with mocha"
	@echo "  mocha-help                  Show this help"
	@echo ""
	@echo "<%>src/assets/report-bugs</%>"
	@echo ""

.PHONY: mocha mocha-help
